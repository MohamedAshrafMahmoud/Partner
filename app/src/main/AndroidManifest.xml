<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mohamed.partner">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/back"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/noActionBar">
        <activity android:name=".View.Splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".View.SignIn"/>
        <activity android:name=".View.SignUp" />
        <activity android:name=".View.MainTab" />
        <activity android:name=".View.AddNewProject" />
        <activity android:name=".View.ProjectDescription" />
        <activity android:name=".View.ShowRates" />
        <activity android:name=".View.userInfo.Profile" />
        <activity android:name=".View.memberInfo.Member_ProfileInfo" />
        <activity android:name=".View.userInfo.SocialAccounts" />
        <activity android:name=".View.memberInfo.Member_SocialAccounts" />
        <activity android:name=".View.userInfo.MyProjects" />
        <activity android:name=".View.ShowComment"/>
    </application>

</manifest>